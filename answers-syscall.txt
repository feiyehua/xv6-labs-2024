Looking at the backtrace output, which function called syscall?
usertrap()

What is the value of p->trapframe->a7 and what does that value represent? 
(Hint: look user/initcode.S, the first user program xv6 starts.)
0x7, calling exec()

What was the previous mode that the CPU was in?
user mode

Write down the assembly instruction the kernel is panicing at. Which register corresponds to the variable num?
80001c9a:	00002683          	lw	a3,0(zero) # 0 <_entry-0x80000000>, a3

Why does the kernel crash? Hint: look at figure 3-3 in the text; 
is address 0 mapped in the kernel address space? Is that confirmed by the value in scause above? 
(See description of scause in RISC-V privileged instructions)

Invalid memory reference of address 0x0, which is not mapped in the kernel address space.
scause=0xd, indicating Load page fault.

What is the name of the process that was running when the kernel paniced? What is its process id (pid)?
initcode, pid = 1